/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.js"], // Use the ** wildcard to include all files in subdirectories
  theme: {
    extend: {},
  },
  plugins: [],
}

